[build-system]
requires = ["pyoz"]
build-backend = "pyoz.backend"

[project]
name = "maxmind"
version = "0.1.0"
description = "A Python module to read MaxMind DB files"
requires-python = ">=3.8"
readme = "README.md"

[tool.pyoz]
module-path = "src/lib.zig"
